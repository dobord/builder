# Digest-bound backtrace inputs must have identical bytes on every CI host.
secure_release/cef_unwind_backtrace.h text eol=lf
tests/fixtures/cef-backtrace/collect_stack_trace.cc text eol=lf

# The qualified acquisition patch verifies the exact public port fixture bytes.
tests/fixtures/cef-qualified-export/portfile.cmake text eol=lf

# Full pinned acquisition inputs are verified byte-for-byte on every OS.
/tests/fixtures/cef-dependency-source/* text eol=lf

# Public pinned FreeRDP regression inputs are byte-verified.
/tests/fixtures/freerdp-host-isolation/** text eol=lf

# The exact vcpkg package hook must remain byte-identical on all runners.
/tests/fixtures/cef-frozen-dependencies/** text eol=lf
